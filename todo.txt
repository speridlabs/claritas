ADD mask of images with yolo and sam

Be able to take a list of videos as input

ADD export of exif to extracted frames

ADD colmap modify support
    - modify via clarity of images
    - modify via separation of images

ADD auto process video

ADD no-interarctive flag

ADD cache store inmediate

al reves 60fp

    1st 
    - group by 10 frames 
    - select 2 best
    1nd
    - group by 12frames
    - select 2 frames
